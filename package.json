{
  "name": "team-task-manager-root",
  "version": "1.0.0",
  "description": "Root package for MERN deployment",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server"
  }
}
